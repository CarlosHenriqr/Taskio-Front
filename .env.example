# Desenvolvimento local: omita VITE_API_URL para usar o proxy Vite (/api → localhost:3333).
# VITE_API_URL=http://localhost:3333

# Produção (Cloudflare Pages → Settings → Environment variables):
VITE_API_URL=https://taskio-api-6vta.onrender.com
