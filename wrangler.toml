# Cloudflare Pages — repo: github.com/CarlosHenriqr/Taskio-Front
# Workers & Pages → Create → Pages → Connect to Git
# Build: npm run build | Output: dist | Node: 20 (.node-version)
# Env (Production): VITE_API_URL=https://taskio-api-0vtm.onrender.com
name = "taskio-front"
compatibility_date = "2024-09-23"
pages_build_output_dir = "./dist"
